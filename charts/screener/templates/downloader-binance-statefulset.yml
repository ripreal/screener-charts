apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-downloader-binance
spec:
  replicas: 1
  serviceName: {{ .Release.Name }}-downloader-binance
  selector:
    matchLabels:
      app: {{ .Release.Name }}-downloader-binance
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-downloader-binance
    spec:
      containers:
        - env:
          - name: DB_HOST
            value: {{ .Release.Name }}-{{ .Values.postgresql.nameOverride }}
          - name: DB_USERNAME
            value: {{ .Values.postgresql.auth.username }}
          - name: DB_PASSWORD
            value: {{ .Values.postgresql.auth.password }}
          - name: DB_DATABASE
            value: {{ .Values.postgresql.auth.database }}
          - name: EXCHANGE
            value: binance
          - name: MRKT_TYPE
            value: spot
          - name: TIMEFRAMES
            value: 1d,1w,1M
          - name: RPM
            value: '100'
          image: {{ .Values.image.repository }}/downloader:{{ .Values.image.tag }}
          imagePullPolicy: Always
          name: {{ .Release.Name }}-downloader-binance-spot
          {{- with .Values.downloaderbinance.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: {{ .Release.Name }}-configs
              mountPath: /usr/src/app/.env
              subPath: downloader.env
              readOnly: false
      hostname: {{ .Release.Name }}-downloader-binance
      volumes:
        - name: {{ .Release.Name }}-configs
          configMap:
            name: {{ .Release.Name }}-configs
      restartPolicy: Always
      imagePullSecrets:
        - name: {{ .Release.Name }}-docker-secrets