apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-listener-binance
spec:
  replicas: 1
  serviceName: {{ .Release.Name }}-listener-binance
  selector:
    matchLabels:
      app: {{ .Release.Name }}-listener-binance
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-listener-binance
    spec:
      containers:
        - env:
          - name: DB_HOST
            value: {{ .Release.Name }}-{{ .Values.postgresql.nameOverride }}
          - name: DB_USERNAME
            value: {{ .Values.postgresql.auth.username }}
          - name: DB_PASSWORD
            value: {{ .Values.postgresql.auth.password }}
          - name: DB_DATABASE
            value: {{ .Values.postgresql.auth.database }}
          - name: EXCHANGE
            value: binance
          - name: TYPE
            value: spot
          image: {{ .Values.image.repository }}/listener:{{ .Values.image.tag }}
          imagePullPolicy: Always
          name: {{ .Release.Name }}-listener-binance-spot
          {{- with .Values.listenerbinance.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: {{ .Release.Name }}-configs
              mountPath: /usr/src/app/.env
              subPath: listener.env
              readOnly: false
        - env:
          - name: DB_HOST
            value: {{ .Release.Name }}-{{ .Values.postgresql.nameOverride }}
          - name: DB_USERNAME
            value: {{ .Values.postgresql.auth.username }}
          - name: DB_PASSWORD
            value: {{ .Values.postgresql.auth.password }}
          - name: DB_DATABASE
            value: {{ .Values.postgresql.auth.database }}
          - name: EXCHANGE
            value: binance
          - name: TYPE
            value: coinm
          image: {{ .Values.image.repository }}/listener:{{ .Values.image.tag }}
          imagePullPolicy: Always
          name: {{ .Release.Name }}-listener-binance-coinm
          {{- with .Values.listenerbinance.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: {{ .Release.Name }}-configs
              mountPath: /usr/src/app/.env
              subPath: listener.env
              readOnly: false
      hostname: {{ .Release.Name }}-listener-binance
      volumes:
        - name: {{ .Release.Name }}-configs
          configMap:
            name: {{ .Release.Name }}-configs
      restartPolicy: Always
      imagePullSecrets:
        - name: {{ .Release.Name }}-docker-secrets